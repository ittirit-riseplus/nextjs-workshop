/* eslint-disable @typescript-eslint/no-unused-vars */
type User = {
  id: number;
  name: string;
  email: string;
  isActive: boolean;
};

const user: User = {
  id: 1,
  name: "Nest",
  email: "nest@example.com",
  isActive: true,
};
